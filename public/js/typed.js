const element = document.getElementById("typed");
const typed = new Typed(element, {
  strings: [
    "Pediaser, cuidando sonrisas desde la infancia: Tu clínica de pediatría de confianza",
  ],
  typeSpeed: 50,
});
